[package]
name = "downloader_cli"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "default-tls"] }
clap = { version = "4.5", features = ["derive"] }
regex = "1.10"
